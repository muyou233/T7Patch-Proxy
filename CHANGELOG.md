# 更新日志

## v3.08（2026-09-15）

- **修复「菜单里保存的设置不生效」的根因**：此前点保存只写进了配置文件和内存，从未推送到引擎 ——
  文件监视器把自己刚写完的时间戳记了账，于是判定"无变化"、跳过了应用。昵称 / 房间密码 / 仅好友
  三个设置全都受影响，改昵称必须重启游戏才生效。现在保存会把这次推送显式交给后台线程
  （引擎调用仍只在允许的线程上执行），约 1 秒内生效（实测 102 毫秒）
- **修复「改昵称后界面不变」**：运行时写入此前只碰 2 个地址，而界面上的名字取自另外 4 个静态
  字符串，两组地址零重叠 —— 没人去更新它，所以界面上的名字永远不变。现在运行时写入镜像那 4 处，
  保存后切换一个界面即刷新
- 主菜单门控**去掉定时兜底**：能打开菜单的只剩一条路径（游戏渲染出主菜单文案 + 玩家已离开标题
  界面）。原先的 60 秒兜底从"前端启动"起算，而前端 UI level 在标题界面就已经是 1 ——
  在「按ENTER开始」挂机满一分钟就会被自动弹窗盖住（用户实测撞到，日志实证）。menu_gate
  配置键一并删除，配置文件里残留的旧行会被静默忽略
- 自动弹出（menu_auto_open=1）在识别到主菜单后**再等 1.5 秒**才出现：判定命中时前端尚未稳定，
  立刻弹出会像"菜单早就在那等你"；延迟后像是对玩家操作的回应。这 1.5 秒内按下快捷键即取消
  本次自动弹出（热键本身不受延迟影响，门控一开就可用）
- **配置系统线程安全重构**：菜单渲染线程 / 游戏更新线程 / 后台监视线程三方同时读写同一份配置，
  此前没有任何同步。现在改为一把锁 + 两条规则（不跨引擎调用持锁、不跨文件读取持锁），
  房间密码缓冲区由堆分配改为固定数组，两个字符串读取接口改为调用方提供缓冲区
- 修复昵称输入框首次为空：配置里昵称为空时会回退读引擎自己的名字，而引擎要等在线档案加载完成
  才写它 —— 旧代码只读一次并锁存，档案加载慢于菜单弹出时整个会话都空着。改为轮询到读到为止，
  玩家一开始输入即停止
- 面板右下角新增 GitHub 仓库图标，点击用默认浏览器打开仓库
- 提示文案清理：不再出现配置文件名，只说明功能；悬停提示只显示"关于"
- 新增诊断日志：`config written to disk` / `settings applied to the engine` /
  `playername applied to the front-end strings` / `first warm-up frame took N ms` ——
  "我改了怎么没生效"这类问题，看一行日志就能定位断在哪一环
- 代码审核修复一批健壮性问题（配置解析、越界读取上界、CRLF 与空值处理等 14 处），
  每处修复都配了回归锚点自检

## v3.07（2026-09-14）

- 新增游戏内控制面板（ImGui 覆盖层）：Present / 窗口过程 / DXGI 工厂链三处 hook，
  BO3 橙黑主题卡片式 UI；46 拦截运行时装卸（MinHook 动态启停）、昵称/密码即时编辑、
  中英双语切换、快捷键自定义；关闭菜单时屏蔽游戏输入，避免误操作角色
- 快捷键只在主菜单真正出现后才生效：通过游戏渲染界面时解析的界面文案判定（标题界面只有
  "按ENTER开始"、连接界面只有"正在连接到在线服务器"，主菜单才出现模式按钮与快捷栏文案），
  在线与离线主菜单均可；前置条件是离开标题界面，另有 60 秒兜底防止菜单被永久锁死
  （menu_gate=0 可关闭兜底）
- 快捷键编辑器兼容中文输入法（被输入法吞掉的按键经扫描码还原为真实键码）
- 内置卡顿修复：进程内拦截旧版 d3dcompiler_46.dll（可在 t7patch.conf 中开关，默认开启）
- 修复 DLC 所有权检查：战役等按钮不再因缓存串味在 DLC 取消勾选后仍显示可用，并校验本地 .xpak 文件
- 崩溃日志升级：记录全部已加载模块，崩溃后可精确定位到模块
- 日志体系：原先的 proxy / block / overlay 三个日志合并为单一 `t7patch.log`（每行带来源标签，
  便于按时间线查看），上限 5 MB 自动轮转（保留一份 .old 备份）；`crashes.log` 保持独立
