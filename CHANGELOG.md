# 更新日志

## v3.08（2026-09-15）

- 修复菜单保存不生效：保存只写了配置文件和内存、没推送到引擎（文件监视器把自己刚写完的时间戳记了账，
  判定"无变化"）。昵称 / 房间密码 / 仅好友都受影响，改昵称必须重启才生效；现在保存会显式推送，约 1 秒内生效
- 修复改昵称后界面不变：运行时写入只覆盖 2 个地址，界面读的是另外 4 个静态字符串；现已同步写入
- 主菜单门控去掉 60 秒定时兜底：兜底从"前端启动"起算，在标题界面挂机满一分钟就会被自动弹窗。
  现在只剩"主菜单文案已渲染 + 已离开标题界面"一条路径；`menu_gate` 键一并删除
- 自动弹出改为识别到主菜单后延迟 1.5 秒（期间按快捷键可取消本次弹出）
- 配置系统线程安全重构：一把锁 + 两条规则（不跨引擎调用持锁、不跨文件读取持锁），房间密码缓冲区改为固定数组
- 修复昵称输入框首次为空（引擎档案加载慢于菜单弹出时，旧代码只读一次就锁存）
- 面板右下角新增 GitHub 仓库图标，点击用默认浏览器打开
- 提示文案不再出现配置文件名；悬停提示只显示"关于"
- 新增诊断日志：`config written to disk` / `settings applied to the engine` /
  `playername applied to the front-end strings` / `first warm-up frame took N ms`
- 代码审核修复一批健壮性问题（配置解析、越界读取上界、CRLF 与空值处理等 14 处）

## v3.07（2026-09-14）

- 新增游戏内控制面板（ImGui 覆盖层）：Present / 窗口过程 / DXGI 工厂链三处 hook，BO3 橙黑卡片 UI；
  46 拦截运行时装卸、昵称/密码即时编辑、中英双语、快捷键自定义；菜单关闭时屏蔽游戏输入
- 快捷键只在主菜单真正出现后生效（按界面渲染时的文案判定，在线离线均可）；前置条件是离开标题界面，
  另有 60 秒兜底防止菜单被永久锁死（menu_gate=0 可关闭兜底）
- 快捷键编辑器兼容中文输入法（被输入法吞掉的按键经扫描码还原）
- 内置卡顿修复：拦截旧版 d3dcompiler_46.dll（t7patch.conf 可开关，默认开启）
- 修复 DLC 所有权检查：不再因缓存串味儿在 DLC 取消勾选后仍显示可用，并校验本地 .xpak 文件
- 崩溃日志升级：记录全部已加载模块
- 日志体系：三个日志合并为单一 `t7patch.log`（每行带来源标签，5 MB 自动轮转保留 .old）；
  `crashes.log` 保持独立
